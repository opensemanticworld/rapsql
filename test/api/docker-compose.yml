version: "3"

services: 
 
  rapsql-api-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: rapsql/rapsql-api-test:latest
    container_name: rapsql-api-test-container
    # environment:
    #   API_BASE_URL: http://pgrst-container:3000
    networks:
      - rapsql-net
    # command: ["python", "-m", "pytest", "api_test.py"]

networks:
  rapsql-net:
    external: true
    driver: bridge