version: "3"

services: 
 
  rapsql-api-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: rapsql/rapsql-api-test:latest
    container_name: rapsql-api-test-container
    networks:
      - rapsql-network
    # command: ["python", "-m", "pytest", "api_test.py"]

networks:
  rapsql-network:
    # external: true
    name: rapsql-network
    driver: bridge