version: "3"

services: 
  rapsql-api-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: rapsql/rapsql-api-test:latest
    container_name: rapsql-api-test-container
    networks:
      - rapsql-net

networks:
  rapsql-net:
    external: true
    driver: bridge